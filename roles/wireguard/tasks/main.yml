---

# - name: Local folders
#   ansible.builtin.import_tasks: local_folders.yml

# - name: Reduce mtu
#   ansible.builtin.import_tasks: reduce_mtu.yml

# - name: Installed
#   ansible.builtin.import_tasks: installed.yml

- name: Facts, delete, add, new server
  ansible.builtin.import_tasks: facts.yml

- name: New server conf
  ansible.builtin.import_tasks: new_server_conf.yml
  # when: wg_new_server
  when: true


# - name: Import key generation tasks
#   ansible.builtin.import_tasks: key_generation.yml

# - name: Import config generation tasks
#   ansible.builtin.import_tasks: config.yml

# - name: Force all notified handlers to run at this point, not waiting for normal sync points
#   ansible.builtin.meta: flush_handlers

# - name: Ensure WireGuard enabled and started
#   ansible.builtin.systemd:
#     name: "{{ wireguard_service_name }}"
#     state: started
#     enabled: true
